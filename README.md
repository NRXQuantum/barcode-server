# barcode-server
Secure Python/Flask barcode API server with multi-key authentication and per-key dynamic rate limits (supports high-throughput test keys). Features sharded CSV storage, in-memory caching, async background image prefetch, SSRF/CSV injection protection, ZIP export, and monitoring. Includes full CLI key management.
